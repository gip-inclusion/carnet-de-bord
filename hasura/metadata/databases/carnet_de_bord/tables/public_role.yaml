table:
  schema: public
  name: role
is_enum: true
array_relationships:
  - name: accounts
    using:
      foreign_key_constraint_on:
        column: type
        table:
          schema: public
          name: account
