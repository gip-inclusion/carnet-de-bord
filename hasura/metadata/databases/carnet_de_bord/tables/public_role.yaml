table:
  name: role
  schema: public
is_enum: true
array_relationships:
  - name: accounts
    using:
      foreign_key_constraint_on:
        column: type
        table:
          name: account
          schema: public
