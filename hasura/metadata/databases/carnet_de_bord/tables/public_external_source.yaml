table:
  name: external_source
  schema: public
is_enum: true
array_relationships:
  - name: external_data
    using:
      foreign_key_constraint_on:
        column: source
        table:
          name: external_data
          schema: public
