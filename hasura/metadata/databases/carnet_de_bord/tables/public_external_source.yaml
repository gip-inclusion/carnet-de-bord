table:
  schema: public
  name: external_source
is_enum: true
array_relationships:
  - name: external_data
    using:
      foreign_key_constraint_on:
        column: source
        table:
          schema: public
          name: external_data
