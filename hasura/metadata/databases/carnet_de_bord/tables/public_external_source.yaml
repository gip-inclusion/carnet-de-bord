table:
  schema: public
  name: external_source
array_relationships:
  - name: external_data
    using:
      foreign_key_constraint_on:
        column: source
        table:
          schema: public
          name: external_data
